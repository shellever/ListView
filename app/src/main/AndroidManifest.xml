<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shellever.listview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EntriesActivity" />
        <activity android:name=".SimpleAdapterActivity" />
        <activity android:name=".BaseAdapterActivity" />
        <activity android:name=".SingleArrayAdapterActivity" />
        <activity android:name=".MultipleArrayAdapterActivity" />
        <activity android:name=".MyListActivity" />
        <activity android:name=".RefreshPagingActivity" />
        <activity android:name=".common.CommonAdapterActivity" />
    </application>

</manifest>